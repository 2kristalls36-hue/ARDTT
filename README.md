# nonameVPN

**Временное имя.** Клиент под Android и сервер на своём VPS: быстрый прямой VPN на AmneziaWG 2.0 и автоматический обход, если до сервера нет прямой видимости.

Подробная техническая схема: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

---

## Зачем этот проект

Обычный VPN (и даже «замаскированный») бесполезен, если оператор режет IP/UDP до вашего VPS или пускает в интернет только «белый список» сервисов.  
**nonameVPN** делает два контура до **вашего** сервера:

1. **Прямой** — AmneziaWG 2.0 (быстро, когда UDP до VPS проходит).
2. **Обход** — трафик идёт через инфраструктуру звонков (TURN), снаружи похож на медиазвонок; внутри — сырые IP-пакеты (режим RAW), без второго WireGuard и без DTLS.

Приложение само на старте оценивает сеть и заранее выбирает метод. Вам остаётся нажать **«Подключить»**.  
По желанию можно включить **«Скрыть свой IP»** — тогда выход в интернет с VPS пойдёт через Cloudflare WARP, а не с адреса VPS.

---

## Как это выглядит для пользователя

Приложение по умолчанию в **режиме пользователя** — только нужное для подключения.  
**Режим администратора** включается в настройках (PIN): тогда доступны подробные логи, деплой сервера и расширенные опции.

1. Ставите сервер на VPS (админ: Compose / деплой из приложения).
2. Пользователь импортирует профиль (QR / файл).
3. Один раз вход в VK — **создать звонок** (hash на телефоне).
4. При запуске — проверка сети; на экране уже выбран метод.
5. «Подключить»; по желанию — «Скрыть свой IP» (WARP на VPS).

Служебное (дозвон, WDTT, workers, сырые логи) в пользовательском режиме не показывается.

---

## Как устроен обход (кратко)

```
Приложения → VPN-туннель на телефоне
                ↓
         шифрование WRAP (как RTP-медиа)
                ↓
         TURN по TCP (релей звонка)
                ↓
         ваш VPS (RAW) → интернет
                         ↘ при «Скрыть IP» → WARP → интернет
```

- Дозвон по умолчанию — **Звонок** (`vkcalls`, обычно без капчи).
- Если не вышло — запасной путь **legacy** (с капчей), без ручного выбора в обычном UX.
- Аккаунт VK нужен, чтобы **создать** звонок; дальше подключение идёт анонимно по сохранённому hash.
- На одно устройство VPN — **один** hash звонка, лежит только на телефоне.
- Если звонок «умер»: по умолчанию спросим; в настройках можно включить тихий автопересоздание.

---

## Сервер (VPS)

Четыре логические части в Docker Compose (имена внутренние):

| Сервис | Назначение |
|--------|------------|
| `direct` | AmneziaWG 2.0 |
| `bypass` | приём RAW/WRAP после TURN |
| `warp` | выход через WARP (wireproxy → tun2socks), без авторестарта контейнера при росте RAM |
| `provision` | пользователи, ключи, единый профиль |

У каждого клиента один `host_id` и одинаковый «хвост» адреса в двух подсетях, например `10.8.0.5` (прямой) и `10.9.0.5` (обход).  
WARP — не третий способ «дозвониться» до телефона, а **выход** с сервера, когда включено скрытие IP.

---

## Клиент (Android)

- База: форк [AmneziaWG for Android](https://github.com/amnezia-vpn/amneziawg-android).
- Обход: адаптация идей и кода [WDTT](https://github.com/amurcanov/proxy-turn-vk-android) (TURN, WRAP, RAW).
- Свой `applicationId` — можно ставить рядом с официальной AmneziaWG.
- Перед подключением: лёгкая проверка сети (без полного поднятия VPN):
  - есть ли интернет (в т.ч. yandex — на «белых списках» часто жив);
  - открытая сеть или ограничения (пробы до крупных сайтов);
  - виден ли VPS по UDP до AmneziaWG;
  - не captive portal ли это.
- На «Подключить» — короткая повторная проверка, затем выбранный path.

---

## Структура репозитория

```
nonameVPN/
├── README.md                 ← вы здесь
├── LICENSE                   ← GNU GPL v3
├── NOTICE                    ← атрибуции исходных проектов
├── docs/
│   └── ARCHITECTURE.md       ← решения и схемы
├── android/                  ← клиент (форк AmneziaWG + bypass) — пока каркас
└── server/                   ← Compose: direct / bypass / warp / provision — пока каркас
```

Код клиента и сервера ещё не перенесён — сейчас зафиксированы архитектура и оформление репозитория.

---

## Статус

| Этап | Состояние |
|------|-----------|
| Архитектура и продуктовые решения | Готово — [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) |
| Оформление репозитория | Готово |
| Серверный Compose + **provision** | Каркас готов — [server/](server/) |
| Образы direct / bypass / warp | Заглушки (бинарники следующим этапом) |
| Android-приложение | **Каркас Compose** — режимы пользователь/админ ([android/](android/)) |
| Имя продукта и схема `nvpn://` | Временные (`nonameVPN`) |

---

## Лицензия и происхождение кода

Проект планируется распространять под **GNU General Public License v3** — см. [LICENSE](LICENSE).

В продукте будут использоваться (с сохранением уведомлений):

- AmneziaWG / компоненты на базе WireGuard — в основном **Apache-2.0** (см. NOTICE);
- наработки WDTT по TURN/WRAP — часто **GPL-3.0**.

Итоговое приложение как комбинированное произведение — под GPL-3; атрибуции перечислены в [NOTICE](NOTICE).

Это не официальный продукт Amnezia, VK или Cloudflare.

---

## Важные ограничения

- Обход зависит от сторонней TURN/API-инфраструктуры звонков: она может измениться или стать недоступной. Прямой AmneziaWG от этого не зависит.
- Режим RAW без DTLS выбран ради скорости и простоты; секрет профиля (пароль) нужно хранить аккуратно.
- Self-hosted: сервер и ключи — ваша ответственность.
- Соблюдайте законы и правила сервисов, которыми пользуетесь.

---

## Документация

- [Архитектура и принятые решения](docs/ARCHITECTURE.md)
- [NOTICE — атрибуции](NOTICE)
- [LICENSE — GPL-3.0](LICENSE)
