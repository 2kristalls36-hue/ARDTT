<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Transparent system bars; Compose theme paints content. -->
    <style name="Theme.NonameVPN" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- Opaque host behind vendor-specific translucent VpnService consent UI. -->
    <style name="Theme.NonameVPN.VpnPermission" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">false</item>
        <item name="android:colorAccent">@color/seed</item>
    </style>
</resources>
