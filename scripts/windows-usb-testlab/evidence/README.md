# Cloud Linux evidence (not a Windows USB run)

These files were produced on Ubuntu x86_64 in Cursor Cloud. They prove Linux toolchain and script tests. They do **not** prove a physical phone, scrcpy, or compatible install onto a user device.

Do not treat paths under `/home/ubuntu` or `/workspace` as the user's PC.
