# ARDTT v0.5.256

Клиент **0.5.256** (`versionCode` 274). Серверный стек **1.0.45** (`DEPLOY_VERSION`).

## 1.0.45 (стек)

- Один установочный архив `ardtt-server-<версия>-linux-<amd64|arm64>.tar.gz`: docker save, `install.sh`, production Compose без `build:`, `bin/docker-compose`. На VPS — `docker load` и `compose up --no-build --pull never`. Нет git/main/GHCR/`get.docker.com`.
- Docker Engine должен быть установлен заранее. Hostnet убран. Ресурсы — labels `com.ardtt.owner` / `com.ardtt.instance`, не префиксы `stack-`.
- Установщик не останавливает Docker, не чистит BuildKit/кэши/swap/журнал хоста, не трогает чужие контейнеры и firewall. Preflight до остановки старого ARDTT. Готовность — `ready.sh`, не один `/health`. Ошибка → откат, без ложного `ARDTT_DONE`.
- Host-порты provision/telemetry отдельно от listen 9100/9200 внутри контейнера. Android зонд arch, качает пакет в файл, SFTP из файла; после установки не делает `docker prune`.
- Совместимость: APK, которые ждут `ardtt-stack-*.tar.gz` или исходники `main`, этот пакет не ставят.

## 0.5.256

- Прямое: AWG создаётся уже на Wi‑Fi (`bindProcessToNetwork` до `awgTurnOn`). На OnePlus `Network.bindSocket` после handshake даёт EPERM — UDP так и уходил через Cloudflare, страницы ползли.
- Direct TUN/awg0 MTU **1200** и TCPMSS 1160, чтобы HTTPS пролезал даже если underlay — WARP. DNS больше не отдаёт AAAA. Обход не менялся.

## 0.5.255

- Прямое: AWG UDP привязывается к живой Wi‑Fi/LTE, а не уходит через чужой VPN (WARP). Handshake был, страницы нет.
- Direct-TUN только IPv4 (`allowFamily(AF_INET)`), чтобы Chrome не ждал мёртвый IPv6. Обход и Auto не менялись.

## 0.5.254

- Прямое (AWG): на VPS `awg0` больше не остаётся с MTU 1420 при клиентском TUN 1280. Handshake проходил, страницы зависали. Ставим MTU 1280 и TCPMSS, как у обхода. Путь Auto/Wi‑Fi и обход не менялись.

## 0.5.253

- Деплой больше не ломает кэш Docker 29: не удаляем lease/снимки BuildKit на живом dockerd (из‑за этого был `snapshot does not exist` / `lease not found` при 4 ГБ свободных). При такой ошибке — полный wipe BuildKit и повтор сборки.

## 0.5.252

- Обновление деплоя на тесном VPS: порог места 900 МБ, swap не съедает запас под сборку, ENOSPC в карточке ошибки вместо списка пакетов apt, без бесполезного `--no-cache` на полном диске.
- Диск больше не копится от сборки к сборке: снимаем orphan Active-снимки и lease-ы BuildKit (Docker 29 / containerd), режем огромные json-логи и старые split-образы; dockerd и чужие контейнеры не трогаем.

## 0.5.251-test

- Гонки Connect/handover: устаревший зонд не перебивает ручной режим, Stop отменяет ожидание probe, Job `finally` не сбрасывает флаги новой операции.
- Очередь смены сети хранит underlay/id, а не одну строку причины. VALIDATED проверяется у выбранной сети; captive Wi‑Fi не бьёт живую LTE.
- Watchdog обхода не рвёт простой: «входящий» — это рост ↓, не TX и не нулевые тики. TURN к VK по умолчанию TCP (как Lab); RAW на сервере по-прежнему UDP.
- Несколько устройств одного профиля получают разные RAW IP; Hide-IP покрывает их. Срок и список устройств в users.json будят RAW через SIGHUP.

## 0.5.250

- Обход на Wi‑Fi больше не рвётся через 3 с: Android валидирует LTE рядом с живым Wi‑Fi — это не смена сети.
- На живом Wi‑Fi TURN идёт по **UDP**, не TCP-в-TCP. На сотовой по-прежнему TCP (оператор часто режет UDP до VK).

## 0.5.249

- Обход (Path B) снова с **3** TURN-воркерами, как в Lab и на provision, а не с 9. На LTE девять параллельных TURN-TCP каналов перемешивают пакеты, внутренний TCP схлопывает окно и страница «ползёт».
- Watchdog больше не рвёт живой обход через ~18 с, если счётчик трафика ещё растёт (раньше всё ниже 200 КБ считалось «только handshake»).

## 0.5.248

- Auto на Wi‑Fi не уходит в обход, если прямое «подумало» и сорвалось: сбой Direct больше не включает Path B. На живом Wi‑Fi остаётся ошибка прямого, а не тихий сброс после ожидания обхода.
- Если система на мгновение не отдаёт TUN (`establish()` = null), Direct повторяет создание интерфейса вместо мгновенного «нет разрешения» и перехода на обход.

## 0.5.247

- Auto на сотовой: Cloudflare считается живым только после TLS или UDP :53, не после TCP `1.1.1.1:443`. VPS — HTTP `/health` 200 без редиректа, не TCP `:9100`. Direct только при открытом Cloudflare. На МТС (Яндекс жив, TCP до Cloudflare и `:9100` есть, TLS и Direct UDP нет) Auto берёт обход, а не мёртвый Direct. TLS-зонд проверяет имя в сертификате; параллельный UDP закрывается, когда TLS уже ответил.
- Auto на Wi‑Fi никогда не берёт обход: живой Wi‑Fi важнее оценки LTE, стартовый зонд и UI не показывают Bypass.
- Логи тестирования на VPS дистрибуции: если nginx уже слушает `:9200`, а socat — `:9199`, установщик больше не ставит `ARDTT_SKIP_TELEMETRY=1`. Gunicorn поднимается внутри контейнера, без publish на хост, и nginx проксирует `POST /api/upload-log` как раньше.

# ARDTT v0.5.246

Клиент **0.5.246** (`versionCode` 264). Серверный стек **1.0.38** (`DEPLOY_VERSION`).

## 0.5.246

- Вкладка «Тест»: хранилище локальных логов и история отправленных — две отдельные вкладки. Комментарий пишется в выезжающем листе. После отправки файл исчезает из хранилища и появляется в истории с номером, который выдаёт сервер. В истории видно, прочитал ли лог автор, и его ответ после разбора.
- Деплой с телефона больше не падает на 7% с «не удалось скачать server/install.sh»: шапка установщика снова в первых 400 символах (это окно, по которому APK 0.5.245 узнаёт скрипт), а `install.sh` берётся из уже скачанного tar, без второго запроса на raw.githubusercontent.com.

# ARDTT v0.5.245

Клиент **0.5.245** (`versionCode` 263). Серверный стек **1.0.37** (`DEPLOY_VERSION`).

## 0.5.245

- Меньше APK: включены R8 и сжатие ресурсов (деплойный стек в APK и раньше не входил — только 7 байт `DEPLOY_VERSION`). У `libclient.so` и `libwg-go.so` срезаны символы (`-s -w`). Ожидаемый arm64 — заметно легче прежних ~62 МБ.

# ARDTT v0.5.244

Клиент **0.5.244** (`versionCode` 262). Серверный стек **1.0.37** (`DEPLOY_VERSION`).

## 0.5.244

- Убрана заглушка закрытой беты: экран «Подтверждение доступа» и код от автора больше не нужны. Приложение открывается сразу.

# ARDTT v0.5.243

Клиент **0.5.243** (`versionCode` 261). Серверный стек **1.0.37** (`DEPLOY_VERSION`).

## 0.5.243

- Обход на каскаде: Path B больше не «оживает keepalive’ами и встаёт на страницах». На входе WARP при каждом heartbeat `users.json` сносил `ip rule` 320/321 (тот же диапазон, что hideIp) — трафик `10.9.0.0/24` на секунды уходил в `eth0` без MASQ. Правила hop теперь 220/221, numeric wipe WARP убран.
- RAW MTU 1280, как `cascade0` и Direct; 1300 не пролезал в hop.
- SIGHUP обхода только при смене паролей, не на LastSeen. Повторный `SetPasswords` с теми же ключами больше не evict’ит AEAD.
- Логи тестирования: если host `:9200` занят nginx дистрибуции, backend публикуется на `:9199` (как ждёт nginx), а не `ARDTT_SKIP_TELEMETRY=1`.

# ARDTT v0.5.242

Клиент **0.5.242** (`versionCode` 260). Серверный стек **1.0.36** (`DEPLOY_VERSION`).

## 0.5.242

- Подписи на плашке вкладок не обрезаются: клип ячейки снят, кегль подгоняется под ширину.
- Виджет больше не открывает приложение на несколько секунд и не сворачивает его.
- Репозиторий открыт: главная страница, деплой и обновления рассчитаны на публичный GitHub. Clone, Releases и API — без PAT. В release APK больше не вшивается `GITHUB_RELEASE_READ_TOKEN`.

# ARDTT v0.5.241

Клиент **0.5.241** (`versionCode` 259). Серверный стек **1.0.36** (`DEPLOY_VERSION`).

## 0.5.241

- Новая подписанная сборка: комментарий на вкладке «Тест» в строке лога, ниже плашка вкладок, виджет включает туннель при закрытом приложении.

# ARDTT v0.5.240

Клиент **0.5.240** (`versionCode` 258). Серверный стек **1.0.36** (`DEPLOY_VERSION`).

## 0.5.240

- Обои туннеля: пять новых сцен (порт, набережная, озеро, маяк, ГЭС), у каждой день / закат / ночь. Киберпанк-город убран.
- Нижняя плашка вкладок ниже: внутренняя высота 48 dp вместо 60.
- Виджет: нажатие при закрытом приложении поднимает процесс, подставляет профиль и включает туннель. Кликать можно по всей плашке, не только по кнопке.

# ARDTT v0.5.239

Клиент **0.5.239** (`versionCode` 257). Серверный стек **1.0.36** (`DEPLOY_VERSION`).

## 0.5.239

- Дроны на экране туннеля — новые спрайты (фронт, ¾ и ближний ракурс) с настоящим альфа-каналом. Винты не вырезались из JPEG с шахматкой: это гладкие полупрозрачные диски, без рваных краёв и клетки в пикселях.
- Круглый значок на устройстве: отдельный adaptive foreground (`ic_launcher_round_foreground`). На minSdk 28 лаунчер берёт XML, а не density-PNG — диск из 0.5.238 на круглой маске не показывался.
- Квадратный adaptive — только буквы (форму даёт маска лаунчера); пластину со сквирклом туда не кладём, иначе рамка снова режется. Буквы крупные, углы DTT внутри сквиркла.
- README: этот репозиторий — канонический источник клиента и стека; телефон берёт `server/` с GitHub, не из APK.
- Режим тестирования: отдельный блок «Комментарий» убран. Текст пишется из поля в строке «Отправить» / «Удалить»; в строке виден обрезанный черновик.

# ARDTT v0.5.238

Клиент **0.5.238** (`versionCode` 256). Серверный стек **1.0.36** (`DEPLOY_VERSION`).

## 0.5.238

- Стек больше не пакуется в APK. Деплой с телефона скачивает `server/` с GitHub (релизный `ardtt-stack-1.0.36.tar.gz` или архив тега) и заливает по SSH. Повторный деплой — та же кнопка «Обновить деплой»: пользователи и ключи в `data/` сохраняются.
- `install.sh` понимает архив репозитория GitHub и сам клонирует тег при `ARDTT_GIT_REF`. CI кладёт архив стека в GitHub Release.
- Из репозитория убран скрипт офлайн-разблокировки закрытой беты (`scripts/alpha-unlock.*`): по коду устройства больше нельзя получить код доступа из исходников.
- Круглый значок лаунчера — отдельный диск: буквы AR/DTT вписаны в окружность, без обрезки квадратной серебряной рамки.
- Карточка клиента: у «Лимит» снова контур, как у соседней кнопки — без обводки она сливалась с фоном.
- Режим тестирования: отдельный блок «Комментарий» убран. Текст пишется из поля в строке «Отправить» / «Удалить»; в строке виден обрезанный черновик.

# ARDTT v0.5.237

Клиент **0.5.237** (`versionCode` 255). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.237

- Значок Ubuntu на карточке сервера снова Circle of Friends: вернули официальные PNG. Упрощённый вектор после правки Beastie выглядел как обычная заглушка.
- Проверил остальные значки ОС: у Fedora и CentOS снял белую/серую пластину; Alpine — горы, не буква A как у Arch; RHEL — шляпа, не «B»; SUSE — Geeko.
- Значки ОС на прозрачном фоне: без цветного диска-подложки у Alpine/RHEL/SUSE/Linux и без пластины у Arch/Fedora/CentOS.
- Карточка сервера: «Онлайн» справа на строке с адресом, под бейджем ОС; деплой слева. Если стек старый — вместо версии «Требуется обновление · …»; четвёртой строки нет. В карточке сервера та же плашка, что в списке.
- Карточка клиента: удаление в меню ⋮; рядом с «Лимит» кнопка «Выключить» (красная) / «Включить» (зелёная); «Устройства: 1/1» и дата подписки справа; IP справа на сером фоне.
- Карточка профиля: вместо host — остаток трафика слева и дата подписки справа; в адресе только IP без порта, каскад как `IP → IP`, IP на сером фоне.
- Карточка сервера: те же серые бейджи IP, каскад `IP → IP`.
- Удаление сервера без связи: предупреждение, что деплой не снимется, и вопрос — удалить только карточку из приложения.
- Режим тестирования: комментарий заполняется на вкладке заранее; окно перед отправкой не показывается, если текст уже есть. Обращения получают номер и видны в истории. Повторная отправка того же лога не создаёт новый номер.

# ARDTT v0.5.236

Клиент **0.5.236** (`versionCode` 254). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.236

- Значок приложения: белое **AR** над бирюзовым **DTT** (градиент cyan→teal) на тёмно-синем поле `#031D3B`. Лаунчер, виджет, QS-тайл и монохром для темы.

# ARDTT v0.5.235

Клиент **0.5.235** (`versionCode` 253). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.235

- Новая подписанная сборка для GitHub Releases (`versionCode` 253), тот же клиент, что 0.5.234.

# ARDTT v0.5.234

Клиент **0.5.234** (`versionCode` 252). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.234

- Интерфейс собран на одной шкале токенов и общих компонентах: одинаковые карточки, ряды, пустые состояния и липкие кнопки на всех вкладках.
- Нижняя плашка вкладок ниже (внутренняя высота 60 dp), кнопка «Подключить» остаётся над ней.
- «Исключения»: заглушки загрузки совпадают с настоящими строками приложений — тот же каркас, иконка, две строки текста и переключатель.
- Значок Ubuntu на карточке сервера — Circle of Friends, не FreeBSD Beastie.
- Тёмная тема: карточки и терминал следуют выбранной теме в настройках, а не только системной.

# ARDTT v0.5.233

Клиент **0.5.233** (`versionCode` 251). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.233

- «Исключения»: заглушки списка приложений совпадают с размерами настоящих карточек (иконка, две строки текста, переключатель Material3).
- Сборка: диалог импорта серверов передаёт `onClick` в `ArdttDialogAction` явно — Android release снова компилируется.

# ARDTT v0.5.232

Клиент **0.5.232** (`versionCode` 250). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.232

- Экспорт серверов: меню ⋮ → «Экспорт», чекбоксы слева для выбора, кнопка «Экспортировать» открывает закрытую ссылку `ardtt://servers?…` с QR и шарингом. Импорт по deep link или из буфера на другом устройстве.
- Деплой: по умолчанию включён **автовыбор портов** — если Direct/Bypass UDP на VPS заняты, `install.sh` подбирает свободные и отдаёт их в `ARDTT_DONE`; приложение сохраняет фактические порты. Выключите переключатель, чтобы задать порты вручную (как раньше). Provision синхронизирует `users.json` с `ARDTT_DIRECT_PORT` / `ARDTT_BYPASS_PORT`.
- Сеть: последняя (выходная) карточка зелёная на прямом пути и синяя на обходе; точка пинга горит 1 с, затем гаснет 0,7 с.
- Карточка сервера: третья строка — «Онлайн»/«Нет связи» (зелёный/красный), деплой по центру (зелёный/жёлтый), мс справа с цветом по пингу; та же шкала пинга во вкладке «Сеть».
- Иконки ОС: PNG для Arch, Ubuntu, Fedora и CentOS; у CentOS отдельный значок (не общий RHEL).

# ARDTT v0.5.231

Клиент **0.5.231** (`versionCode` 249). Серверный стек **1.0.35** (`DEPLOY_VERSION`).

## 0.5.231

- Режим «Авто» на Wi‑Fi всегда берёт прямое подключение и не переключается на обход по результатам зонда. Проба и запасной обход остаются для мобильной сети. Ручной «Обход» на Wi‑Fi по-прежнему идёт через обход.
- Android release снова собирается: у `DeployEngine` вернули `activeTargetId` (его случайно заменили трекером каскадных хопов в 0.5.228).

# ARDTT v0.5.230

Клиент **0.5.230** (`versionCode` 248). Серверный стек **1.0.34** (`DEPLOY_VERSION`).

## 0.5.230

- Карточки на всех экранах с той же тонкой обводкой по контуру, что у «Оформление» и «Поддержка автора»: 2.dp, цвет primary с прозрачностью 16%. Списки серверов, клиентов и профилей больше без «голой» тени. Сеть и каскадный прогресс по-прежнему красят ободок серым/зелёным по смыслу хопа.

# ARDTT v0.5.229

Клиент **0.5.229** (`versionCode` 247). Серверный стек **1.0.34** (`DEPLOY_VERSION`).

## 0.5.229

- Установка на VPS с ~1 ГБ RAM больше не падает на `rm: Device or resource busy` в `/var/lib/docker/buildkit/.../rootfs`. Сброс BuildKit сначала останавливает docker/containerd, снимает overlay executor, и `rm` не обрывает `install.sh`. «Удалить сервер» тоже umount'ит `/var/lib/docker` до wipe — повторная установка каскада после удаления доходит до сборки.

# ARDTT v0.5.228

Клиент **0.5.228** (`versionCode` 246). Серверный стек **1.0.33** (`DEPLOY_VERSION`).

## 0.5.228

- Деплой / обновление: полоса прогресса без синей точки на конце трека (stop indicator Material3) и без разрыва между заливкой и дорожкой. Заполнение анимируется, а не прыгает скачками.
- Карточка сервера: каскад на строке IP как `вход → выход`, не через тире.
- «Сеть»: палочка между карточками того же серого, что обводка серых карточек (не primary).
- «Сеть»: зелёная обводка только у конечного хопа из настроек — VPS 2 при каскаде, CloudFlare при «Скрыть IP».
- Каскадный деплой / обновление: два ряда с IP (VPS 1 и VPS 2). Второй ряд серый, пока стек на нём не установлен, затем зелёный. Этап с ролью и адресом, процент под полосой.
- Карточка сервера: у каскадного VPS SSH user по умолчанию `root` и поле PEM, как у первого. Поля нижней половины формы поднимаются над клавиатурой.
- README и DEPLOY: стек ставится из приложения **или** `git clone` тега релиза (`v0.5.228` / стек 1.0.33). Публичный репозиторий не требует PAT на VPS; архив в APK для сервера тогда необязателен.

# ARDTT v0.5.227

Клиент **0.5.227** (`versionCode` 245). Серверный стек **1.0.33** (`DEPLOY_VERSION`).

## 0.5.227

- «Удалить сервер»: на выделенном VPS снимаются и следы Docker в iptables/ip6tables (`DOCKER*` chains, MASQ `docker0`/`br-*`, raw DROP), ufw-правила даже при inactive ufw, `/etc/containerd`, `/swapfile` и apt-пакеты Docker.

# ARDTT v0.5.226

Клиент **0.5.226** (`versionCode` 244). Серверный стек **1.0.33** (`DEPLOY_VERSION`).

## 0.5.226

- «Удалить сервер»: полный wipe. Снимаются контейнеры, образы ARDTT, leftover TUN/iptables/ip-rule, правила ufw/firewalld, `/opt/ardtt`. Если на VPS нет чужих контейнеров — также Docker Engine, `docker0`, `/swapfile` установщика и apt-репозиторий Docker.

# ARDTT v0.5.225

Клиент **0.5.225** (`versionCode` 243). Серверный стек **1.0.33** (`DEPLOY_VERSION`).

## 0.5.225

- Сервер: единый контейнер `ardtt` вместо семи `network_mode: host`. По умолчанию своя netns и публикация только портов ARDTT — деплой на уже занятом VPS не перехватывает iptables/sysctl/ip-rule чужих сервисов и не останавливает dockerd, если рядом крутятся другие контейнеры. Запасной режим `ARDTT_NETWORK_MODE=hostnet`.
- Стек 1.0.33: каскадный выход снова слушает UDP (`ARDTT_ROLE` → cascade), учётка WARP живёт в `data/warp`, cascade не трогает `rp_filter` чужих интерфейсов.

# ARDTT v0.5.224

Клиент **0.5.224** (`versionCode` 242). Серверный стек **1.0.31** (`DEPLOY_VERSION`).

## 0.5.224

- Вкладка «Сеть»: заполненные карточки живут, пока VPN подключён. Уход на другую вкладку больше не запускает проверку заново. Сброс — только после отключения. Потянуть вниз — обновить данные карточек.

# ARDTT v0.5.223

Клиент **0.5.223** (`versionCode` 241). Серверный стек **1.0.31** (`DEPLOY_VERSION`).

## 0.5.223

- Вкладка «Серверы»: «Удалить» сначала предупреждает, что стек снимется с VPS, затем по SSH останавливает контейнеры и стирает `/opt/ardtt`. Карточка пропадает только после успеха.

# ARDTT v0.5.222

Клиент **0.5.222** (`versionCode` 240). Серверный стек **1.0.31** (`DEPLOY_VERSION`).

## 0.5.222

- Карточка сервера: каскад на строке IP как `вход-выход`. Если стек не установлен — «Установить деплой»; в параметрах — «Переустановить деплой».

# ARDTT v0.5.221

Клиент **0.5.221** (`versionCode` 239). Серверный стек **1.0.31** (`DEPLOY_VERSION`).

## 0.5.221

- Вкладка «Серверы»: «Не установлено», если SSH проходит, а стек на VPS не отвечает; «Нет связи», если не удаётся даже подключиться.

# ARDTT v0.5.220

Клиент **0.5.220** (`versionCode` 238). Серверный стек **1.0.31** (`DEPLOY_VERSION`).

## 0.5.220

- Админ: в быстрых настройках туннеля — переключатель профилей. Строка «Профиль: ФИО» над карточкой убрана.

# ARDTT v0.5.219

Клиент **0.5.219** (`versionCode` 237). Серверный стек **1.0.31** (`DEPLOY_VERSION`).

## 0.5.219

- «Добавить на этот телефон» сразу ставит текущее устройство в список привязанных, не дожидаясь VPN.

# ARDTT v0.5.218

Клиент **0.5.218** (`versionCode` 236). Серверный стек **1.0.30** (`DEPLOY_VERSION`).

## 0.5.218

- Параметры сервера: на сохранённой карточке кнопка только «Переустановить сервер». Ход переустановки — выезжающий лист снизу, как у «Обновить деплой», а не лог в форме.
- Новый клиент больше не занимает слот устройства в момент создания. Привязка — только после реального подключения.

# ARDTT v0.5.217

Клиент **0.5.217** (`versionCode` 235). Серверный стек **1.0.29** (`DEPLOY_VERSION`).

## 0.5.217

- Вкладка «Сеть»: каскад VPS 1 → VPS 2 только из live `/health` или карточки деплоя. Без угадывания по WAN и соседним серверам.

# ARDTT v0.5.216

Клиент **0.5.216** (`versionCode` 234). Серверный стек **1.0.29** (`DEPLOY_VERSION`).

## 0.5.216

- Вкладка «Сеть»: карточки VPS 1 → VPS 2 только при реальном каскаде (live `/health` или карточка деплоя). Другой WAN, соседний сервер и устаревший флаг каскада больше не рисуют ложный hop.

# ARDTT v0.5.215

Клиент **0.5.215** (`versionCode` 233). Серверный стек **1.0.29** (`DEPLOY_VERSION`).

## Коротко

- Официальная расшифровка: **ARDTT** — Amnezia & Raw Dial over TURN Tunnel.
- В коде больше нет идентификатора **nonamevpn**: пакет `com.ardtt.app`, VPS `/opt/ardtt`, контейнеры `ardtt-*`, переменные `ARDTT_*`, протокол установщика `ARDTT_PROGRESS|…`.
- Смена `applicationId` — это **новое приложение** для Android: обновление поверх старого APK не встанет, нужна переустановка.
- Установщик переносит `/opt/nonamevpn` → `/opt/ardtt` и снимает контейнеры `nvpn-*`. Старые `NVPN_*` в окружении и `.env` ещё читаются.
- Каскад: Hide-IP WARP ставится на **выходном** VPS; без галочки выход идёт в WAN выхода, не в Cloudflare. После одиночных обновлений входа/выхода починены утечка DNS каскада и ключи WARP.
- Обход: быстрее failover на белом списке; Hide-IP больше не расходится с фактическим Cloudflare-egress; на малых VPS — Docker 29 snapshot и prune build-cache перед обновлением стека.
- UI: знак AR/DTT, бейджи Ubuntu (Circle of Friends) и Debian, баннер доната, QS-тайл профиля, карточки hop на «Сети», заблокированные действия профиля при поднятом VPN, терминальный хром логов деплоя; с карточки сервера убрана мигающая ping-точка.
- Режим тестирования — в общих Настройках, без PIN администратора; вкладка «Тест» в любом режиме UI.
- In-app обновления смотрят в GitHub Releases репозитория **ARDTT**; APK режутся по ABI (`arm64-v8a`, `armeabi-v7a`, `x86_64`, universal).

## Каскад и Hide-IP

- Телефон знает только входной VPS. WAN и Hide-IP WARP — на **выходе** (`GET /v1/hide-ip-prefixes` у входа через `10.10.0.1`). Вход в каскаде — passthrough, свои `/32` на `warp0` не вешает.
- Hide-IP выкл. → egress с WAN выхода (не pop Cloudflare рядом со входом). Это относится и к Direct, и к Bypass.
- После standalone-обновления одного из двух VPS: DNS каскада больше не утекает мимо hop; ключи WARP на выходе переживают обновление.
- DNS `:53` по-прежнему `ip rule` prio 100 → `main`, не через WARP. Hide-IP на лету: `ip rule` + conntrack flush, без рестарта клиентского TUN. WARP-контур — `wireproxy` + `tun2socks`.

## Обход и деплой на VPS

- Медленный Bypass-failover на белом списке (Yandex жив, Cloudflare мёртв): probe не принимает TCP `:9100` за живой Direct; Auto уходит в обход.
- Несовпадение Hide-IP и фактического Cloudflare-egress устранено вместе с переходом WARP на wireproxy/tun2socks.
- Docker 29 на малых дисках: snapshot-промахи при пересборке `bypass` больше не роняют обновление; лишние deploy-файлы с хоста убираются.
- Перед обновлением VPS из приложения сбрасывается Docker build cache, чтобы на маленьком диске сборка доходила до конца.
- Логи установки/обновления в приложении — тот же терминальный хром, что вкладка «Журналы». С листа обновления сервера нельзя случайно смахнуть в залипший overlay.

## Интерфейс

- Лаунчер, QS и тайлы: белое **AR** над оранжевым **DTT** на `#181E25`.
- Карточка сервера: знак дистрибутива (Ubuntu CoF / Debian и др.) и версия ОС в бейдже, без пустой заглушки и без мигающей ping-точки.
- Баннер «Поддержка автора» на туннеле (пока подключено) и карточка в Настройках; ссылка [Спасибо Мир](https://spasibomir.ru/pay/34807).
- Quick Settings: тайл переключения VPN-профиля (не только старт/стоп).
- «Сеть»: hop-карточки с серой обводкой до загрузки, затем заливка и ping; карточка провайдера остаётся, даже если underlay lookup не удался.
- Пока VPN поднят, действия профиля выглядят заблокированными, а не исчезают.
- Тема оформления остаётся на экране во время записи теста.

## Тестирование и обновления

- Переключатель «Режим тестирования» в общих Настройках (после соглашения). Вкладка «Тест» доступна в любом режиме UI, без PIN администратора.
- Проверка обновлений: GitHub Releases `2kristalls36-hue/ARDTT`, fallback на старый `update.json` на VPS дистрибуции.

## Поставка

- GitHub Release: `ardtt-0.5.215-{arm64-v8a,armeabi-v7a,x86_64,universal}.apk`, `ardtt-update.json`, `SHA256SUMS.txt`.
- Сборка: workflow `.github/workflows/android-release.yml` с `main` / тега `v*`.
- Gradle по-прежнему в `android/`, не в корне репозитория.

## Рекомендации

- На открытой сети, если UDP до VPS проходит, оставляйте **Авто** / прямое — так быстрее.
- На белом списке ожидайте **обход** (TURN). Один hash на устройство; мёртвый звонок — recreate в настройках или диалог на Туннеле.
- Hide-IP — это egress, не третий клиентский путь. DNS туннеля не должен идти через WARP.
- Каскад: сначала ставьте **выход**, затем **вход**. Телефон коннектится только ко входу.

## Предыдущие релизы 0.5.20x

| Релиз | Суть |
|-------|------|
| 0.5.231 | Авто на Wi‑Fi всегда прямое; сборка APK: вернули `activeTargetId` |
| 0.5.230 | Обводка карточек как у «Оформление» / «Поддержка автора» |
| 0.5.229 | Стек 1.0.34: сброс BuildKit на 1 ГБ VPS не падает на busy overlay; uninstall umount'ит Docker перед wipe |
| 0.5.228 | Полоса деплоя; каскад двумя рядами; «Сеть» серая линия и зелёный конечный хоп; форма каскада: root, PEM, клавиатура |
| 0.5.227 | Uninstall: iptables/ip6tables Docker, ufw при inactive, containerd dirs |
| 0.5.226 | Uninstall полностью снимает Docker/swap, если на VPS нет чужих контейнеров |
| 0.5.225 | Единый контейнер деплоя (isolated netns); стек 1.0.33 чинит cascade/WARP |
| 0.5.224 | «Сеть»: карточки сохраняются до отключения VPN |
| 0.5.223 | «Удалить» сервер: uninstall на VPS, затем карточка |
| 0.5.222 | Карточка сервера: IP каскада `вход-выход`; «Установить деплой» |
| 0.5.221 | «Серверы»: «Не установлено» vs «Нет связи» |
| 0.5.220 | Админ: переключатель профилей в быстрых настройках туннеля |
| 0.5.219 | «Добавить на этот телефон» сразу привязывает устройство |
| 0.5.218 | Переустановка листом снизу; создание клиента без автопривязки устройства |
| 0.5.217 | «Сеть»: каскад только из health / карточки деплоя |
| 0.5.216 | «Сеть»: без ложного каскада VPS 1 → VPS 2 |
| 0.5.213 · 0.5.214 · 0.5.215 | Каскад Hide-IP на выходе, ping-точка с карточки сервера, пакет `com.ardtt.app` |
| 0.5.212 | Docker snapshot bypass на малых VPS |
| 0.5.211 | GitHub-обновления → репозиторий ARDTT |
| 0.5.210 | Сброс Docker build cache при обновлении VPS |
| 0.5.209 | Ubuntu Circle of Friends на карточке сервера |
| 0.5.208 | DNS/WARP каскада после standalone-деплоя |
| 0.5.207 | Терминал деплоя, бейдж ОС, донат в тёмной теме |
| 0.5.206 | Bypass на БС и Hide-IP mismatch |
| 0.5.205 | QS-профиль, ABI-split, Hide-IP off = WAN выхода |
| 0.5.203 · 0.5.201 | Знак AR/DTT, hop-карточки, баннер доната |
| 0.5.200 | Режим тестирования в общих Настройках |

Живые сборки: [Releases](https://github.com/2kristalls36-hue/ARDTT/releases) (`v0.5.247` latest, `v0.5.246`).
